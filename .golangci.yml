version: "2"
linters:
  default: all
  disable:
    - depguard
    - exhaustruct
    - forbidigo
    - gochecknoglobals
    - gochecknoinits
    - godot
    - ireturn
    - mnd
    - wsl           # Deprecated - replaced by wsl_v5
  enable:
    - wsl_v5        # New version of wsl
  settings:
    tagliatelle:
      case:
        rules:
          json: snake
        use-field-name: true
    varnamelen:
      ignore-names:
        - err
        - ok
        - idx
    revive:
      rules:
        - name: var-naming
          disabled: true
    wsl_v5:
      allow-first-in-block: true
      allow-whole-block: false
      branch-max-lines: 2
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  settings:
    gci:
      sections:
        - standard
        - prefix(github.com/uzumaki-inc/stay_or_go)
        - default
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
